{
  "name": "algotrack-worker",
  "version": "1.0.0",
  "description": "Worker for AlgoTrack - processes async tasks (messages, PDF generation)",
  "author": "Algoritmika",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "prisma generate && tsc",
    "start": "node dist/index.js",
    "start:dev": "prisma generate && ts-node src/index.ts",
    "start:watch": "ts-node-dev --respawn src/index.ts",
    "prisma:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.7.1",
    "bullmq": "^5.1.0",
    "ioredis": "^5.3.2",
    "axios": "^1.6.2",
    "puppeteer": "^21.6.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "prisma": "^5.7.1",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}

