# 🚀 Инструкция по запуску AlgoTrack

## ⚠️ Важно: Docker Desktop должен быть запущен!

Перед запуском убедитесь, что **Docker Desktop запущен и работает**.

Проверка:
```powershell
docker ps
```
Если команда работает без ошибок - Docker запущен ✅

---

## Вариант 1: Полный запуск через Docker (рекомендуется)

### Шаг 1: Запустите Docker Desktop
Откройте Docker Desktop и дождитесь полной загрузки.

### Шаг 2: Запустите проект
```powershell
docker-compose up -d
```

### Шаг 3: Инициализируйте БД
```powershell
# Подождите 30 секунд для запуска сервисов
Start-Sleep -Seconds 30

# Миграции
docker exec algotrack-backend npx prisma migrate dev --name init

# Тестовые данные
docker exec algotrack-backend npx prisma db seed
```

### Шаг 4: Откройте приложение
- Frontend: http://localhost:5173
- Backend API: http://localhost:3001
- API Docs: http://localhost:3001/api

**Тестовые данные:**
- Email: `alexander@algoschool.org`
- Пароль: `teacher123`

---

## Вариант 2: Локальный запуск (только БД через Docker)

Если Docker Desktop запущен, но хотите запустить backend/frontend локально:

### Шаг 1: Запустите только БД и Redis
```powershell
docker-compose -f docker-compose.db-only.yml up -d
```

### Шаг 2: Установите зависимости
```powershell
# Backend
cd backend
npm install
npx prisma generate
cd ..

# Frontend
cd frontend
npm install
cd ..

# Worker (опционально)
cd worker
npm install
npx prisma generate
cd ..
```

### Шаг 3: Создайте .env файлы

**backend/.env:**
```
DATABASE_URL="postgresql://algotrack:algotrack_password@localhost:5432/algotrack"
REDIS_URL="redis://localhost:6379"
JWT_SECRET="test-secret-key"
GREEN_API_ID=""
GREEN_API_TOKEN=""
PORT=3001
NODE_ENV=development
```

**frontend/.env:**
```
VITE_API_URL=http://localhost:3001
```

**worker/.env:**
```
DATABASE_URL="postgresql://algotrack:algotrack_password@localhost:5432/algotrack"
REDIS_URL="redis://localhost:6379"
GREEN_API_ID=""
GREEN_API_TOKEN=""
PDF_DIR="./uploads/pdfs"
```

### Шаг 4: Инициализируйте БД
```powershell
cd backend
npx prisma migrate dev --name init
npx prisma db seed
cd ..
```

### Шаг 5: Запустите сервисы в 3 терминалах

**Терминал 1 - Backend:**
```powershell
cd backend
npm run start:dev
```

**Терминал 2 - Frontend:**
```powershell
cd frontend
npm run dev
```

**Терминал 3 - Worker (опционально):**
```powershell
cd worker
npm run start:dev
```

---

## Быстрая проверка

После запуска проверьте:

1. **Health check:**
   ```powershell
   curl http://localhost:3001/api/health
   ```

2. **API документация:**
   Откройте в браузере: http://localhost:3001/api

3. **Frontend:**
   Откройте в браузере: http://localhost:5173

---

## Остановка

```powershell
# Остановить все контейнеры
docker-compose down

# Или только БД
docker-compose -f docker-compose.db-only.yml down
```

---

## Проблемы?

### Docker не запускается
- Убедитесь, что Docker Desktop установлен
- Перезапустите Docker Desktop
- Проверьте, что виртуализация включена в BIOS

### Ошибки подключения к БД
- Проверьте, что PostgreSQL контейнер запущен: `docker ps`
- Проверьте логи: `docker logs algotrack-postgres`

### Порт занят
- Измените порты в `docker-compose.yml`
- Или остановите другие сервисы, использующие порты 3001, 5173, 5432, 6379



