# 📋 Пошаговая инструкция по запуску AlgoTrack

## ⚠️ ВАЖНО: Сначала запустите Docker Desktop!

1. Откройте **Docker Desktop** на вашем компьютере
2. Дождитесь полной загрузки (иконка в трее должна быть зеленая)
3. Только после этого выполняйте команды ниже

---

## 🚀 Быстрый запуск (рекомендуется)

### Шаг 1: Проверьте Docker
```powershell
docker ps
```
Если команда работает без ошибок - Docker запущен ✅

### Шаг 2: Запустите проект
```powershell
cd C:\Users\akbar\AlgoTrack
docker-compose up -d
```

### Шаг 3: Подождите 30 секунд
Дождитесь запуска всех контейнеров.

### Шаг 4: Инициализируйте базу данных
```powershell
# Выполнение миграций
docker exec algotrack-backend npx prisma migrate dev --name init

# Заполнение тестовыми данными
docker exec algotrack-backend npx prisma db seed
```

### Шаг 5: Откройте приложение

**Frontend (веб-интерфейс):**
- Откройте браузер: http://localhost:5173

**Backend API (для тестирования):**
- API документация: http://localhost:3001/api
- Health check: http://localhost:3001/api/health

### Шаг 6: Войдите в систему

**Тестовые данные для входа:**
- **Email:** `alexander@algoschool.org`
- **Пароль:** `teacher123`

---

## 📊 Просмотр логов

Если что-то не работает, проверьте логи:

```powershell
# Все сервисы
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только frontend
docker-compose logs -f frontend
```

---

## 🛑 Остановка проекта

```powershell
docker-compose down
```

---

## ❌ Если Docker Desktop не запускается

### Решение 1: Перезапустите Docker Desktop
1. Закройте Docker Desktop полностью
2. Откройте снова
3. Дождитесь загрузки

### Решение 2: Проверьте виртуализацию
- Убедитесь, что виртуализация включена в BIOS
- Включите WSL2 в Windows Features

### Решение 3: Переустановите Docker Desktop
Скачайте с официального сайта: https://www.docker.com/products/docker-desktop

---

## 🔍 Проверка работы

После запуска проверьте:

1. **Контейнеры запущены:**
   ```powershell
   docker ps
   ```
   Должны быть видны: algotrack-postgres, algotrack-redis, algotrack-backend, algotrack-frontend

2. **Backend отвечает:**
   ```powershell
   curl http://localhost:3001/api/health
   ```
   Должен вернуть: `{"status":"ok",...}`

3. **Frontend открывается:**
   Откройте http://localhost:5173 в браузере

---

## 📝 Что дальше?

После успешного запуска:

1. ✅ Войдите в систему с тестовыми данными
2. ✅ Откройте класс "Нейросети - Группа А"
3. ✅ Создайте карточку для ученика
4. ✅ Проверьте генерацию сводки

---

## 💡 Полезные команды

```powershell
# Перезапуск сервиса
docker-compose restart backend

# Просмотр логов конкретного сервиса
docker-compose logs -f backend

# Остановка всех сервисов
docker-compose down

# Остановка с удалением данных (ОСТОРОЖНО!)
docker-compose down -v
```

---

## 🆘 Нужна помощь?

Если что-то не работает:
1. Проверьте, что Docker Desktop запущен
2. Проверьте логи: `docker-compose logs`
3. Убедитесь, что порты 3001, 5173, 5432, 6379 свободны
4. Перезапустите контейнеры: `docker-compose restart`



